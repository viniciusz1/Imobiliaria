import { ImobFilterPipe } from './imob-filter.pipe';

describe('ImobFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ImobFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
